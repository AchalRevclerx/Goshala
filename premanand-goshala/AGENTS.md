# Project Rules

- Never read the contents of any `.env` file. This includes `.env`, `.env.local`, `.env.production`, or any other environment/secret file. You may read `.env.example` or reference variable names, but never open or display actual secret values.
- Never commit `.env` files or real secrets to the repository.
